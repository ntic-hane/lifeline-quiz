特定技能1号 ライフライン・設備 学習アプリ 仕様書
日本トータル情報事業協同組合（N.T.I.C）
最終更新：2026-07-31
---
1. 概要
特定技能1号「建設分野／ライフライン・設備」の受験者向け学習アプリ。
技能実習生・特定技能候補者が来日前後にスマートフォンで自習することを想定している。
項目	内容
公開URL	https://ntic-hane.github.io/lifeline-quiz/
リポジトリ	GitHub `ntic-hane/lifeline-quiz`（Public）
ホスティング	GitHub Pages（Branch: `main` / `/(root)`）
構成	`index.html` 1枚のみ（約200KB）
外部依存	なし（画像もbase64で埋め込み、CDN・APIを一切使わない）
費用	無料。登録・インストール不要
設計思想
手軽さを最優先する。 ログイン制は採用しない。本人確認の役に立たない割に、離脱を招く
丸暗記させない。 出題順・選択肢順を毎回シャッフルする
母語で理解し、日本語で慣れる。 本試験は日本語のみで出題されるため、母語と日本語の併記表示を用意する
---
2. 収録内容
全140問。すべてN.T.I.Cのオリジナル作成（→ 6章 コンテンツポリシー）。
学科（3区分共通範囲）60問
分野	コード	問題数
安全衛生	`safe`	22
施工管理	`mgmt`	14
法令・現場のルール	`rule`	12
図面・基礎	`base`	12
実技（ライフライン・設備）80問
分野	コード	問題数
工具・計測	`tool`	18
電気	`elec`	23
通信	`com`	16
配管・設備	`pipe`	23
本試験の仕様（アプリはこれに準拠）
	学科	実技
出題範囲	土木・建築・ライフラインの3区分共通	区分別
問題数	30問	20問
試験時間	60分	40分
合格基準	65%以上	65%以上
方式	CBT・2〜4択および○×	同左
学科テキストは1〜4のうち1のみ区分別、2〜4は共通。したがって学科には
土木・建築と共通の内容（安全衛生・施工管理・法令・図面）が含まれる。
---
3. 機能
3.1 模試モード
本番と同じ条件で通しで解く。
学科30問60分／実技20問40分、65%で合否判定
途中で正誤・解説を表示しない（採点後に一括表示）
カウントダウンタイマー。残り5分で赤表示
問題番号グリッドで任意の問題へジャンプ。回答済みは色が変わる
時間切れで自動採点。結果画面に「時間切れ」を表示
未回答が残った状態で採点する場合は確認ダイアログ
中断可。ただし中断した回の成績は記録しない（本番同様の扱い）
3.2 練習モード
分野を選択して出題（複数選択可）
ランダム10問 ／ 全問
1問ごとに即座に正誤・解説を表示
中断は確認なしで即ホームへ（そこまでの記録は保存される）
3.3 学習の記録
端末内に保存（→ 5章）。ホーム画面上部に常時表示。
学習ずみ問題数 / 全140問、通算正答率、のべ回答数
分野別正答率バー（65%未満はオレンジ＝合格ライン割れ）
模試履歴 直近5件（日付・区分・得点・65%以上は緑）
苦手なところを練習する：誤答率が高い順に10問抽出。未学習問題も混ぜる
CSV書き出し（問題ID別の出題数・正解数・正答率＋模試履歴）
記録の全消去
3.4 多言語
日本語 / English / Bahasa Indonesia / Tagalog の4言語。
ヘッダー右のボタンで即時切替。画面状態は保持される
外国語選択時は「日本語も表示」トグルが出る（既定ON）。母語と日本語を併記
翻訳が未作成の問題は自動的に英語で表示（表示欠けを起こさない）
---
4. データ仕様
4.1 問題データ構造
```js
{
  c: "safe",                    // 分野コード
  q: [ja, en, id, tl],          // 問題文（言語順は LANGS と一致）
  o: [                          // 選択肢（3〜4個）
       [ja, en, id, tl],
       ...
     ],
  a: 0,                         // 正解のインデックス（配列内の位置）
  e: [ja, en, id, tl],          // 解説
  id: "G1"                      // ビルド時に自動採番
}
```
4.2 問題ID
ビルド時に配列順で自動採番する。学科 `G1`〜`G60`、実技 `J1`〜`J80`。
> **問題を追加するときは必ず配列の末尾に足すこと。**
> 途中に挿入するとIDがずれ、利用者の学習記録が別の問題に紐づいてしまう。
4.3 選択肢の順序
データ上の並び順は変更してはならない。 正解を `a`（インデックス）で保持しているため、
順序を入れ替えると正解がずれる。表示時のシャッフルはアプリ側で行っている。
翻訳レビューを依頼する際も、この旨を必ず伝えること（レビュー用紙には明記済み）。
---
5. 学習記録の保存
`localStorage` に保存。サーバーへの送信は一切ない。
キー	`ntic_lifeline_v1`
```js
{
  v: 1,
  q: { "G1": {n:3, c:2, t:1690000000} },   // 出題数, 正解数, 最終回答時刻
  exams: [ {d:timestamp, p:"g", c:22, n:30, pct:73} ]   // 直近20件
}
```
制約
端末・ブラウザをまたがない（別端末には引き継がれない）
シークレットモードでは保存されない（その場合アプリが警告を表示し、メモリのみで動作）
iOS Safariは長期間未使用でデータを破棄することがある。ホーム画面に追加すると消えにくい
保存できない環境では自動でメモリ動作に切り替わる（クラッシュしない）
> **キー名 `ntic_lifeline_v1` は変更禁止。** 変更すると既存利用者の記録が読めなくなる。
> データ構造を変える場合は `v` を上げ、旧構造からの移行処理を書くこと。
---
6. コンテンツポリシー
JAC（建設技能人材機構）の公開教材・公開問題の文章、選択肢、解説は一切使用していない。
参照してよいもの（著作物ではなく事実情報）：
試験区分の構成、出題範囲、試験時間・問題数・合格基準
各テキストの章立て（＝どの単元が範囲か）
専門用語そのもの
禁止：
問題文・選択肢・解説の転載、および言い換えによる実質的な複製
教材の構成をなぞった再構成（翻案にあたるおそれ）
作問は「出題範囲に含まれる知識」から独自に書き起こす。
アプリ内および配布物には、オリジナル作成である旨を明記する。
---
7. ファイル構成とビルド
作業ディレクトリ：`/home/claude/lifeline/`
ファイル	役割
`shell.html`	UI・ロジック・CSS。`/*__DATA__*/` `/*__UI__*/` を差し込み口として持つ
`ui.js`	UI文言辞書（`LANGS` / `T` / `CAT`）。言語追加時はここに文言を足す
`data_g.js` `data_g2.js`	学科問題（32 + 28）
`data_j.js` `data_j2.js` `data_j3.js`	実技問題（32 + 8 + 40）
`tl_g.js` `tl_j.js`	タガログ語訳（言語ごとに独立ファイル）
`build.py`	上記を結合して `index.html` を生成
`make_review.js`	通訳レビュー用 対訳表（画面閲覧用）
`make_review_print.js`	通訳レビュー用 対訳表（A4印刷・赤入れ欄つき）
`make_flyer.py`	A4両面チラシ
ビルド
```bash
python3 build.py        # → index.html
node --check check.js   # 構文チェック（build.pyが生成）
```
`build.py` は生成時に以下を自動検査する。
問題数と各言語の翻訳数の一致（不足は英語フォールバックの旨を表示）
必須パーツの存在（モーダルCSS・確認ダイアログ・戻るボタン・ロゴ・統計機能）
想定外の文字種の混入（ハングル・キリル文字）
言語を追加する手順
翻訳ファイルを作る（`xx_g.js` / `xx_j.js`）。配列の順序を問題データと厳密に一致させる
`build.py` の `EXTRA` に1行追加
`ui.js` の `LANGS` と全UI文言に該当言語を追加
ビルド
---
8. 実装上の禁止事項
過去に実際の不具合を起こした事項（→ `BUGFIX.md`）。
禁止	理由
`confirm()` `alert()` `prompt()` の使用	プレビュー環境やアプリ内ブラウザでブロックされ、操作不能になる。自前の `ask()` を使うこと
`apple-mobile-web-app-status-bar-style` に `black-translucent`	iPhoneでヘッダーが時計・ノッチと重なる
`screen` `name` `top` などブラウザ標準と同名のグローバル変数	衝突する。本アプリでは `screen` → `view` にリネーム済み
localStorageを直接読み書きする	使用不可の環境でクラッシュする。必ず `load()` / `save()` 経由
表示サイズと同倍率の画像を埋め込む	高精細画面で潰れる。3倍解像度で用意する
表記「NTIC」	正式表記は N.T.I.C（ドット入り）。内部キー `ntic_lifeline_v1` のみ例外
検証方法
`jsdom` でUIを通しでテストできる。
```bash
npm install jsdom
node t.js        # 画面遷移
node t2.js       # confirm/alert が使えない環境での動作
node t4.js       # モーダルの多重表示・固定表示
node t3.js       # 時間切れの自動採点
node test_persist.js   # 保存 → 再訪時の復元
```
`confirm` / `alert` を `throw` させた状態でもテストすること。
---
9. デザイン
用途	色
ベース（濃紺）	`#0F2E52`
アクセント（青）	`#1E4D8C`
注意喚起（黄）	`#F2C230`
正解	`#12795A`
誤答・警告	`#C0392B`
背景	`#EDF1F6`
ヘッダー下のハザードストライプ（黄×紺の斜線）は建設現場の意匠として全体の識別要素
ロゴは白版を使用（濃紺背景のため）
アプリアイコンはキューブマークを濃紺の正方形に配置。ホーム画面名は `N.T.I.C Academy`
---
10. 更新手順
ローカルで `build.py` を実行し `index.html` を生成
ダウンロードフォルダに古い `index.html` があれば先に削除する
（残っているとブラウザが `index (1).html` にリネームし、GitHub Pagesが404になる）
GitHubリポジトリで Add file → Upload files → 上書き
ファイル一覧が `index.html` 1個だけであることを確認
1〜2分でPagesに反映。スマホは一度アプリを閉じて開き直す
学習記録はURLに紐づくため、更新しても消えない。
