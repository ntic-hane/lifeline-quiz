バグ修正履歴
特定技能1号 ライフライン・設備 学習アプリ
記録開始：2026-07-31（初版開発日）
各項目は 症状 → 原因 → 対処 → 再発防止 で記録する。
「原因」を残すことに意味があるので、直したことだけ書かないこと。
---
表示・レイアウト
#1 iPhoneでヘッダーが時計・ノッチに重なり、言語ボタンが押せない
症状：ホーム画面から起動すると、ロゴと言語ボタンがステータスバーの下に潜り込む。ロゴが見えず、言語切替も操作できない
原因：`<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">` を設定していた。この値はステータスバーを透過させ、コンテンツを画面最上端から描画させる
対処：値を `default` に変更。あわせて `@supports` 付きで `padding-top: calc(12px + env(safe-area-inset-top))` を追加し、ノッチ・ダイナミックアイランドを回避
再発防止：`black-translucent` は使わない。`viewport-fit=cover` を使う場合は必ず `safe-area-inset-*` で余白を確保する
#2 ロゴが潰れて読めない
症状：ヘッダーのロゴが滲み、「Nihon Total Information Cooperative」の文字が判読不能
原因：表示サイズ（18px）とちょうど同じ解像度の画像を埋め込んでいた。iPhoneの高精細画面では実質3倍の画素が必要
対処：3倍解像度（高さ69px）の画像に差し替え、表示は23pxに拡大
再発防止：埋め込み画像は表示サイズの3倍で用意する
#3 確認ダイアログが画面下部に積み上がる
症状：「← ホーム」を押すと、画面中央ではなくページ下端に「やめますか？」がテキストとして表示される。押すたびに増えていく
原因：モーダル用CSSの挿入処理で、置換対象の文字列が存在せず無言で失敗していた。スタイルが当たらないため、通常のブロック要素として本文末尾に配置されていた
対処：`</style>` の直前に確実に挿入。あわせて要素側にも `position:fixed` 等を直接指定して二重化。既存モーダルを削除してから生成する処理を追加
再発防止：`build.py` に必須パーツの存在検査を追加（モーダルCSS・確認ダイアログ・戻るボタン・ロゴ・統計機能）。以後ビルドのたびに「必須パーツ：すべて有り」が出力される
#4 CSSクラス名の衝突
症状：ヘッダーに意図しない余白と文字サイズが適用される
原因：分野別正答率バーに付けた `.bar` が、ヘッダーの `<div class="bar">` と同名だった
対処：`.fbar` にリネーム
再発防止：汎用的な単語のクラス名を新設しない
---
操作・遷移
#5 開始すると途中でやめられない
症状：練習・模試を始めると、最後まで解き終えるまでホームに戻れない
原因：問題画面に離脱手段を用意していなかった。単純な設計漏れ
対処：問題画面の左上に戻るボタンを追加。練習は即ホーム、模試は確認ダイアログを経て中断
再発防止：新しい画面を作るときは、必ず「そこから抜ける導線」を同時に用意する
#6 「←」が前の問題に戻ると誤解される
症状：戻るボタンを実装したのに、利用者が存在に気付かない／別機能だと思う
原因：ラベルが「←」のみ。模試モードには別に「前へ」ボタンがあるため、いっそう紛らわしかった
対処：「← ホーム」に変更（各言語対応）。枠線を紺にして視認性を上げた
再発防止：アイコンのみのボタンは行き先を併記する
#7 確認ダイアログが出る場面で何も起きない
症状：模試中に「← ホーム」を押しても反応しない。記録の消去も動かない
原因：ブラウザ標準の `confirm()` を使っていた。プレビュー環境やサンドボックス化されたiframe、一部のアプリ内ブラウザでは `confirm()` がブロックされ、常に `false` を返す。「キャンセルされた」と判定されて処理が中断していた
対処：自前の確認ダイアログ `ask()` を実装し、`confirm()` / `alert()` を全廃。時間切れ通知も `alert()` をやめ、結果画面に赤字で表示する方式に変更
再発防止：`confirm()` `alert()` `prompt()` は使用禁止。テスト時はこれらを `throw` させた状態で通しテストする（`t2.js`）
#8 言語を切り替えると解説が消える
症状：練習モードで回答後に言語を変えると、正誤表示と解説が消え、選択肢も操作できなくなる
原因：言語切替で画面を再描画する際、回答済み状態を復元していなかった
対処：フィードバック描画を関数に切り出し、再描画時に直近の回答から状態を復元
再発防止：状態を持つ画面は、再描画で状態が失われないか必ず確認する
---
ロジック
#9 「苦手なところを練習」に誤答した問題が出ない
症状：間違えた問題があるのに、未学習の問題ばかり抽出される
原因：未学習問題の正答率を暫定 `0.5` として扱っていたため、「半分正解した問題」と同率になり、同率時は出題回数が少ない未学習が優先されていた
対処：未学習を `0.55` として、誤答のある問題より後、正解済みより前に位置づけ。同率時は出題回数が多い順（何度も間違えている問題を優先）に変更
再発防止：優先順位のロジックは実データを入れてテストする（`test_core.js`）
#10 未訳の問題で表示が壊れるおそれ
症状（潜在）：翻訳ファイルの件数が問題数に満たない場合、該当問題の訳が `undefined` になる
原因：翻訳マージ処理が、対応する訳がない場合に何もせず抜けていた。結果として言語ごとの配列長が不揃いになる
対処：訳がない場合は英語で埋めるフォールバックを実装。ビルド時に件数の不一致を検出して警告を出す
再発防止：ビルド出力に「未訳あり（学科-0 実技-40）→ 英語で表示」と明示される
#11 グローバル変数名の衝突リスク
症状（予防的対処）：画面状態を保持する変数名が `screen` だった
原因：`window.screen` はブラウザ標準のグローバルプロパティ。環境によっては代入が無視され、画面遷移が動かなくなるおそれがある
対処：`view` にリネーム
再発防止：`screen` `name` `top` `status` `length` などはグローバル変数名に使わない
---
データ・ビルド
#12 問題データに別言語の文字が混入
症状：日本語の選択肢にハングル、ロシア語が紛れ込んでいた（「가만히 我慢して続ける」「открытый掘削」）
原因：作問時の入力ミス
対処：該当箇所を修正
再発防止：ビルド時にハングル・キリル文字の混入を検査。「混入チェック：なし」が出力される
#13 ビルドスクリプトの構文エラー
症状：`build.py` が `SyntaxError: f-string` で停止
原因：f-string の中にJavaScriptのコードを埋め込んだ際、波かっこをエスケープしていなかった
対処：`{{` `}}` にエスケープ
再発防止：f-string内にJSを書く場合は波かっこの二重化を確認する
#14 文字列置換の無言失敗
症状：#3 の根本原因。置換対象が見つからなくてもエラーにならず、変更が反映されないまま完了する
対処：置換処理に `assert` を入れ、対象が存在しない場合は停止させるようにした
再発防止：スクリプトによる置換は必ず `assert` で対象の存在を確認する
---
公開・運用
#15 GitHub Pagesが404になる
症状：Pagesを有効にしてもサイトが表示されない
原因：アップロードしたファイル名が `index (1).html` になっていた。パソコンのダウンロードフォルダに同名ファイルが残っていると、ブラウザが自動的に連番を付ける
対処：GitHub上でファイル名を `index.html` にリネーム
再発防止：ダウンロード前に古いファイルを削除する。アップロード後はファイル一覧が `index.html` 1個だけであることを確認する
#16 更新したのに古い画面のまま
症状：ファイルを差し替えても、リポジトリに `index.html` と `index (2).html` が並存し、古いほうが表示され続ける
原因：#15 と同じ理由で連番が付いたまま、新旧2ファイルが存在した
対処：古い `index.html` を先に削除してから、新しいファイルをリネーム（逆順だと名前が衝突してリネームできない）
再発防止：更新後は必ずファイル一覧を確認する
---
記録の書き方
新しい不具合を直したら、以下を追記すること。
```markdown
### #番号 症状を一行で

- **症状**：利用者から見て何が起きたか
- **原因**：なぜそうなったか（ここが最も重要）
- **対処**：何を変えたか
- **再発防止**：同じ種類の失敗を防ぐ仕組み。可能ならビルド時検査やテストに落とす
```
同じ失敗を繰り返さないための記録なので、原因を省略しないこと。
